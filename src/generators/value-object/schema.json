{
	"$schema": "http://json-schema.org/schema",
	"$id": "ValueObject",
	"title": "Create a value object",
	"type": "object",
	"properties": {
		"name": {
			"type": "string",
			"description": "Specifies the name of the generated files.",
			"$default": {
				"$source": "argv",
				"index": 0
			},
			"x-prompt": "What name would you like to use for the generated file?"
		},
		"path": {
			"type": "string",
			"format": "path",
			"$default": {
				"$source": "workingDirectory"
			},
			"description": "The path at which to create the files.",
			"visible": false
		},
		"project": {
			"type": "string",
			"description": "The name of the project.",
			"$default": {
				"$source": "projectName"
			},
			"x-dropdown": "projects"
		},
		"flat": {
			"type": "boolean",
			"default": false,
			"description": "When true, creates the new files at the top level of the current project."
		}
	},
	"required": ["name", "project"]
}
